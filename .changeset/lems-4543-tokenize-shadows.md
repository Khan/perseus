---
"@khanacademy/perseus": patch
---

Tokenize remaining hardcoded colors: interactive-graph drop shadows and the
movable-point tooltip shadow now use the Wonder Blocks shadow token (fixing
shadows not adapting in dark mode), the error-boundary icon uses the critical
foreground token, and the plotter scale box border uses the neutral border
token.
