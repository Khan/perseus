---
"@khanacademy/perseus-editor": major
---

The `cb` and `silent` parameters of the `EditorPage`'s `onChange` prop have been removed. Consumers of `EditorPage` should remove any usages of these parameters.
