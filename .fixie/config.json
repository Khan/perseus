{
    "verify": "pnpm --silent tsc && pnpm --silent tesc && pnpm --silent linc",
    "fix": "pnpm --silent fixc",
    "todoPatterns": [
        "(^| )TODO-NEXT(:|\\([a-z])",
        "(^| )TODO(:|\\([a-z])"
    ]
}
