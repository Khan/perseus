import {render, screen} from "@testing-library/react";
import {Mafs} from "mafs";
import * as React from "react";

import type {Interval} from "mafs";

describe("PointGraph", () => {
    it("adds a point when clicking anywhere on the graph", () => {
        // Arrange: Render a graph

        // Act: click mouse at 1,1

        // Assert: a point was added to the graph

        expect(true).toBe(false);
    });
});
